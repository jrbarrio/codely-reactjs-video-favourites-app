import React from 'react';
import PropTypes from 'prop-types';

const Footer = () => (
    <div className="footer-content">
        <div className="footer-title-text" >Created by: Jorge Roldan Barrio</div>
    </div>
);

export default React.memo(Footer);
